apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.app_name }}
  description: Sample Python application named ${{ values.app_name }}
  # Example for optional annotations
  annotations:
    github.com/project-slug: jtandy13/${{ values.app_name }}
spec:
  type: service
  owner: development
  lifecycle: experimental